$kind: http-example
request:
  url: "{{baseUrl}}/contacto?select=string"
  method: POST
  headers:
    - key: Prefer
      value: resolution=merge-duplicates
      description: Preference
    - key: Content-Type
      value: application/json
  queryParams:
    - key: select
      value: string
      description: Filtering Columns
  body:
    type: json
    content: |-
      {
        "id": 6721,
        "nombre": "string",
        "tecnologias": [
          "string",
          "string"
        ]
      }
response:
  statusCode: 201
  statusText: Created
  body:
    type: text
    content: ""
order: 1000
