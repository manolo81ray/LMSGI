$kind: http-request
name: /descripcion
url: "{{baseUrl}}/descripcion?select=string"
method: POST
headers:
  - key: Prefer
    value: resolution=merge-duplicates
    description: Preference
  - key: Content-Type
    value: application/json
queryParams:
  - key: select
    value: string
    description: Filtering Columns
body:
  type: json
  content: |-
    {
      "id": 4077,
      "saludo": "string",
      "nombre": "string",
      "biografia": "string",
      "subtitulo": "string",
      "foto": "string",
      "curriculum_url": "string"
    }
examples: ./.resources/-descripcion-1.resources/examples
order: 2000
